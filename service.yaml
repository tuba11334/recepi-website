apiVersion: v1
kind: Service
metadata:
  name: recipe-app-service
spec:
  #type: NodePort
  type: LoadBalancer
  selector:
    app: recipe-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080